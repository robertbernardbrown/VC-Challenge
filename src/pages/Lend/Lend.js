import React from "react";
import { Link } from 'react-router-dom';

const Lend = () => (
    <React.Fragment>
        <Link to={'/'} id="rent-btn">HOME</Link>
    </React.Fragment>
)

export default Lend;